# Multiple weighted imputation

The R code contains functions which permit to do multiple imputations on single missing variables by classification and regression trees or random forests with weighted units.
The other variables (without any NA) will all be included in the imputation model
